{
  "name": "prezance-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Prezance marketing site — Vite (vanilla JS) with client-side routing and page transitions.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "playwright": "^1.60.0"
  }
}
